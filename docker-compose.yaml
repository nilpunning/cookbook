version: '3.8'

services:
  hallertau:
    build: .
    restart: unless-stopped
    volumes:
      - ./hallertau.toml:/hallertau.toml:ro
      - ./recipes:/recipes:rw
    ports:
      - "127.0.0.1:7004:8080"
